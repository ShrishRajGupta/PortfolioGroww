# Developer ergonomics only. Never active in deployments.
spring:
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    com.example.demo: debug
